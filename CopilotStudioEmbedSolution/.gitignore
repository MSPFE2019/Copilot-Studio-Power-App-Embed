# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# msbuild output directories
/bin
/obj

# MSBuild Binary and Structured Log
*.binlog

# PCF control build output copied in for local `pac solution pack` packaging
# (regenerated from CopilotStudioEmbed/out via `npm run build`; not committed)
/src/Controls/
